HOST=0.0.0.0
PORT=8090
HOOK_BEARER_TOKEN=replace-me

# This service should run on a Proxmox node or jump host.
PCT_BIN=pct
QM_BIN=qm

# Make runtime defaults
MAKE_IMAGE=ghcr.io/timberlandhosting/make-runtime:latest
POSTGRES_IMAGE=postgres:16-alpine
MAKE_TIMEZONE=UTC
BACKUP_RETENTION_DAYS=7
